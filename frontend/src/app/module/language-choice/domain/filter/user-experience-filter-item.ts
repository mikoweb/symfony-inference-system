export default class UserExperienceFilterItem {
  constructor(
    public readonly langId: string,
    public readonly langName: string,
    public readonly levelName: string,
    public readonly levelLabel: string,
  ) {}
}
